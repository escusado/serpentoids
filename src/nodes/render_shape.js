Class(Serpentoids.Nodes, "ShapeRender").inherits(Serpentity.Node)({
    types : {
        display_shape : Serpentoids.Components.DisplayShape,
        position : Serpentoids.Components.Position
    }
});
