Class(Serpentoids.Nodes, "Movement").inherits(Serpentity.Node)({
    types : {
        position : Serpentoids.Components.Position,
        motion : Serpentoids.Components.Motion,
        motion_controls : Serpentoids.Components.MotionControls
    }
});
